import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#fff',
    },

    secondary: {
      main: '#2D82BE',
    },
  },
});

export default theme;
